<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini_project</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center">
  <div class="w-full max-w-md bg-zinc-950 p-10 rounded-xl shadow-lg">
    <h1 class="text-3xl mb-10  text-center font-semibold">Login To your Account</h1>
    <form action="/login" method="POST" class="flex flex-col gap-4 w-full mb-10">
        <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800 w-full  mb-3" type="email" placeholder="Email"   name="email">
        <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800 w-full  mb-3" type="password"  placeholder="Password"   name="password">
        <input  class="px-5 py-2 rounded-md bg-blue-500 hover:bg-blue-600 transition-colors w-full cursor-pointer font-medium" type="submit" value="Login Account">
    </form>
  </div>
</div>

</body>
</html>